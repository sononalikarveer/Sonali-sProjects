import math

num = float(input("Enter the number: "))

# Ceil = math.ceil(num)
# floor = math.floor(num)
# roundoff = round(num)
#
# print( "Ceil:",Ceil,"\nfloor:", floor, "\nroundoff:", roundoff,)


sqroot=math.sqrt(num)
square=num**2
cbroot=math.cbrt(num)

print( "sqroot:",sqroot,"\nsquare:", square, "\ncbroot:", cbroot,)